[package]
name = "tsc-trace-viewer"
version = "0.6.0"
edition = "2021"
license = "MIT"
description = "viewer for binary output of tsc-trace"

[dependencies]

[dependencies.sdl2]
version = "0.36.0"
features = ["ttf"]
